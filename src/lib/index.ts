// place files you want to import through the `$lib` alias in this folder.

// AG Grid components
// We now import AgGrid directly from ag-grid-svelte5-extended
// The local Grid.svelte component has been replaced with the imported version
import { AgGrid } from 'ag-grid-svelte5-extended';
export { AgGrid as Grid };
